export type ShopProfile = {
  id: string;
  shopName: string;
  whatsappNumber: string;
  telegramUsername?: string | null;
  description?: string | null;
  logoUrl?: string | null;
  createdAt: string;
};
